---
- name: Converge
  hosts: all
  
  vars:
    gitlab_restart_handler_failed_when: false

  pre_tasks:
    - name: Remove the .dockerenv file so GitLab Omnibus doesn't get confused.
      file:
        path: /.dockerenv
        state: absent

  tasks:
    - name: "Test ansible-role-gitlab"
      include_role:
        name: "ansible-role-gitlab"

